cmake_minimum_required(VERSION 3.0.2)
project(lexium_cobot_with_final_gripper)
find_package(catkin REQUIRED)
catkin_package()
install(DIRECTORY urdf meshes launch rviz DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION})
